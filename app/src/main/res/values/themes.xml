<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Note" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.Note" parent="Base.Theme.Note" />
    <style name="Theme.App.SplashNote" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/light_blue_ballerina</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/pinned_notes</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Note</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>
</resources>